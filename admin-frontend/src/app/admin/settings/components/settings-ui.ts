import type { CSSProperties } from "react";

export const settingsSectionStyle = {
  borderWidth: 0,
} satisfies CSSProperties;

export const settingsFieldStyle = {
  borderWidth: 1,
  boxShadow: "none",
} satisfies CSSProperties;

export const settingsSectionClassName = "bg-transparent";

import React from 'react';
import clsx from 'clsx';
import { type I{{ pascalCase name }}Props } from './{{ kebabCase name }}.types';
import styles from './{{ kebabCase name }}.module.scss';
import use{{pascalCase name}}Hooks from './{{ kebabCase name }}.hooks';
import Page from '@components/elements/page';

const {{ pascalCase name }} = (props: I{{ pascalCase name }}Props) => {
  const { className, style } = props;
  const hooks = use{{pascalCase name}}Hooks(props);

  return (
  <Page className={clsx('{{ kebabCase name }}', styles.{{ camelCase name }}, className)} style={style}>
    Welcome to {{ pascalCase name }} page
  </Page>
  );
}

export default React.memo({{ pascalCase name }});

import React from 'react';

export interface I{{ pascalCase name }}Props extends React.PropsWithChildren<object> {
    className?: string;
}

export interface I{{ pascalCase name }}Store {
    count: number;
    increment: () => void;
    decrement: () => void;
    incBy: (amount: number) => void;
    decBy: (amount: number) => void;
}

export interface I{{ pascalCase name }}ContextType {
    value: number;
    increment: () => void;
    decrement: () => void;
}


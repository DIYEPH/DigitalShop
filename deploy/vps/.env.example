# Image tags pushed by GitHub Actions. The backend-bot image bundles the bot
# runner (bot_binance_pay); BotRunnerManager runs one instance per shop.
ADMIN_FRONTEND_IMAGE=ghcr.io/OWNER/REPO/admin-frontend:latest
ADMIN_BACKEND_IMAGE=ghcr.io/OWNER/REPO/admin-backend:latest
BACKEND_BOT_IMAGE=ghcr.io/OWNER/REPO/backend-bot:latest

# Host ports on the VPS.
# admin-frontend is public. Backends are bound to 127.0.0.1 by docker-compose.
ADMIN_FRONTEND_HOST_PORT=4100
ADMIN_BACKEND_HOST_PORT=3000
BACKEND_BOT_HOST_PORT=3001

# admin-frontend
ADMIN_FRONTEND_API_ORIGIN=http://admin-backend:3000

# Shared Neon PostgreSQL connection string.
DATABASE_URL=postgresql://USER:PASSWORD@HOST.neon.tech/DB?sslmode=require

# admin-backend
JWT_SECRET=change-this-to-a-long-random-secret-at-least-32-chars
JWT_EXPIRES_IN=3d
REFRESH_TOKEN_EXPIRES_IN=30d
CORS_ORIGIN=https://admin.example.com
BCRYPT_ROUNDS=12
ENABLE_RATE_LIMITING=true
MAX_REQUESTS_PER_MINUTE=1000
ADMIN_LOG_LEVEL=info
ADMIN_LOG_FORMAT=json
HEALTH_CHECK_TIMEOUT=5000

# AES-256-GCM key for per-shop credentials/secrets.
# Must be identical in admin-backend (encrypt) and backend-bot (decrypt).
SHOP_SECRET_ENCRYPTION_KEY=change-this-to-a-long-random-encryption-key

# backend-bot internal auth (dev/seed fallback; each shop stores its own
# secret in telegram_bots via admin Settings).
BOT_INTERNAL_SECRET=change-this-shared-bot-secret

# BotRunnerManager: reconciles bot instances against ACTIVE telegram_bots.
BOT_RUNNER_ENABLED=true
BOT_RUNNER_POLL_MS=15000

# backend-bot background jobs.
PENDING_ORDER_SYNC_ENABLED=true
PENDING_ORDER_SYNC_POLL_MS=30000
PENDING_ORDER_SYNC_BATCH_SIZE=100

# Points / referral (shared by all shop bots).
DAILY_LOGIN_POINTS_REWARD=5
DAILY_LOGIN_TIMEZONE=Asia/Ho_Chi_Minh
REFERRAL_REFERRER_BONUS=1
REFERRAL_REFEREE_BONUS=1

# Per-shop values are NOT configured here — sellers manage them in admin
# Settings: bot token/secret (telegram_bots), shop name and support link
# (shops), Binance Pay / SePay / bank (shop_payment_credentials).
